display:
  default:
    display_title: Výchozí
    display_options:
      fields:
        bundle:
          label: Typ
        operations:
          label: Operace
      pager:
        options:
          expose:
            items_per_page_options_all_label: '- Vše -'
      exposed_form:
        options:
          exposed_sorts_label: 'Řadit dle'
      filters:
        bundle:
          expose:
            label: Typ
